let arr = [100, 200, 300, 500, 0, 900, 50, 1000, 20, 4000];
 // Let arr 3 means 100,200,300 is a subarray and then 200,300,500 is another subarray so i need to just calculate the maz sum
 // of the given array

let k = 3;
let sum = 0;
let n = arr.length;
for (let i = 0; i<)
